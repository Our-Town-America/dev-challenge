version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dev-challenge
    ports:
      - "8080:80"
    volumes:
      - ./app:/var/www:rw
    restart: unless-stopped
